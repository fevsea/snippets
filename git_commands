# WORKING WITH SSH KEYS
eval "$(ssh-agent -s)" # Start ssh-agent Needed?
ssh-add -k ~/.ssh/id_rsa #permanently add ssh key
git config --global url."git@github.com:".insteadOf "https://github.com/"

#Stress gpu (nvidia)
__GL_SYNC_TO_VBLANK=0 glxgears
